#include "main.h"

/**
 * _isdigit - it checks for digit
 *
 * @c: the input value of the _isupper function
 *
 * Return: Always 0
 *
 */


int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
		return (0);

}
